server:
  port: 9090
  servlet:
    context-path: /api/v1
spring:
  application:
    name: supermercado
  datasource:
    url: jdbc:mysql://localhost:3306/supermercado
    username: root
    password: paulo3144615 #cambiar contraseña
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
      database-platform: org.hibernate.dialect.MySQLDialect

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernxxaate.type.descriptor.sql.BasicBinder: TRACE

security:
  jwt:
    secret-key: alht7XYKujQPw1ourB0c4rIRg4x6RNrqewufShlZoug=
    tokenExpiration: 600000